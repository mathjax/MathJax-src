export enum ConfigElement {
  HANDLER = 'handler',
  FALLBACK = 'fallback',
  ITEMS = 'items',
  TAGS = 'tags',
  OPTIONS = 'options',
  NODES = 'nodes',
  PREPROCESSORS = 'preprocessors',
  POSTPROCESSORS = 'postprocessors',
  INIT = 'init',
  CONFIG = 'config',
  PRIORITY = 'priority',
  PARSER = 'parser',
}


export enum ConfigHandler {
  DELIMITER = 'delimiter',
  MACRO = 'macro',
  CHARACTER = 'character',
  ENVIRONMENT = 'environment',
}
